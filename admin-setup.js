document.querySelector('#setup').addEventListener('submit',async e=>{e.preventDefault();const s=document.querySelector('#status');s.textContent='Activating…';try{const r=await fetch('/api/admin/bootstrap',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({token:document.querySelector('#token').value})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Activation failed.');const me=await fetch('/api/me',{credentials:'same-origin'}).then(x=>x.json());localStorage.setItem('nv_user',JSON.stringify({name:me.user.name,email:me.user.email,role:me.user.role,server:true}));s.textContent='Admin activated. Returning to NearVibe…';setTimeout(()=>location.href='/',800)}catch(x){s.textContent=x.message}});
